# ============================================================
#  .gitignore —— 发到 GitHub 前请确认这几类都不进版本库
# ============================================================

# ---- 编译产物（Release 由 CI 现场生成，不提交二进制）----
dist/*
!dist/.gitkeep
*.pdb
*.user
*.suo
bin/
obj/
packages/

# ---- 第三方工具：无权再分发，且配置里写死了本机绝对路径 ----
#  需要番茄下载能力时，用户自己准备 exe，程序会引导选择并复制到 fanqie-core\ 下
fanqie-core/
fanqie-core.ini

# ---- 运行期数据 ----
#   下载的正文有版权（现在 dist/下载 里已有 ~57 MB，绝不能进历史）
#   缓存 json 含站点数据；日志含本机信息
下载/
dist/下载/
dist/cache/
*.log
logs/

# ---- 临时调试产物 / 批量下载日志 ----
_*.txt
_*.exe
/tmp/
